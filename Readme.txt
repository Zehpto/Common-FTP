Installation Notes:

1. pip install termcolor

2. Add hosts to "addresses" one per line.

Two files, "common_usernames" and "ftp_password" have common usernames and passwords. Feel free to add/remove from either file.
